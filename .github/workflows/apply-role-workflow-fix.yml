name: Apply role workflow patch

on:
  push:
    paths:
      - '.github/workflows/apply-role-workflow-fix.yml'
      - 'scripts/apply-role-workflow-fix.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: python3 scripts/apply-role-workflow-fix.py
      - name: Validate diff
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/v8/App.tsx src/v8/WorkDetail.tsx src/v8/TeamTree.tsx
          git commit -m "Fix approval hierarchy and clean topology connectors"
          git push origin HEAD:main
